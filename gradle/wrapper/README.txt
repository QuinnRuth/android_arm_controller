# Gradle Wrapper 说明

本项目支持使用 `gradlew` / `gradlew.bat` 在命令行构建。

如果你在 Windows PowerShell 中：

- `.\gradlew.bat -v`
- `.\gradlew.bat assembleDebug`

注意：需要本机有可用的 Java 运行环境（建议 JDK 17，与 Android Gradle Plugin 8.x 匹配）。
